#machine:
#  services:
#    - docker

dependencies:
  cache_directories:
    - "~/docker"
  override:
    #- if [[ -e ~/docker/image.tar ]]; then docker load -i ~/docker/image.tar; fi
    #- docker build -t imerso/slettmeg:test .
    #- mkdir -p ~/docker; docker save imerso/slettmeg:test > ~/docker/image.tar
    - if [[ ! -e ~/docker/test.txt ]]; then mkdir ~/docker/ ; echo "is there not"; echo "asdf" >> ~/docker/test.txt; else echo "not existing"; fi

  #test:
  #override:
  #  - docker run -d imerso/slettmeg:test

